import React from "react";
import { View } from "react-native";

const Line = (props) => {
  return (
    <View
      style={{
        backgroundColor: "grey",
        width: "100%",
        height: 1,
        marginTop: 8,
      }}
    ></View>
  );
};

export default Line;
